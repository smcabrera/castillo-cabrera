#!/bin/sh

echo some useful gems:
gem install viddl-rb
gem install timetrap
gem install tmuxinator
gem install mechanize
gem install jekyll
gem install gem-ctags
gem install shotgun
gem install homesick

sudo apt-get install tilda

